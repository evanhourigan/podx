# Create a .venv if absent and activate it
if [ ! -d .venv ]; then
  python3 -m venv .venv
fi
source .venv/bin/activate

has dotenv && dotenv

# Helpful prompt hint
export VIRTUAL_ENV_DISABLE_PROMPT=1

# Keep PYTHONPATH clean for editable installs
unset PYTHONPATH